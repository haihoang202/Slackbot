#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../urban/bin/urban" "$@"
  ret=$?
else 
  node  "$basedir/../urban/bin/urban" "$@"
  ret=$?
fi
exit $ret
